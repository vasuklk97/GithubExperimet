name: Experiment

on:
  workflow_run:
    workflows: [ "Main Workflow" ]
    types:
      - workflow_run
  
          
env:
  actions: Vishnu

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name
      run: |
        echo ${{ env.actions }}
