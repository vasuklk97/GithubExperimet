name: Experiment

on:
  push:
    branches:
      - 'master'
    paths:
      - 'GithubExperiment/GithubExperiment/Controllers/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name
      run: |
        echo "Vasu"
