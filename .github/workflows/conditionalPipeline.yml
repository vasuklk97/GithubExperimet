name: Experiment

on:
  push:
    branches:
      - 'master'
    paths:
      - './GithubExperimet/Controllers/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name
      run: |
        echo "Vasu"
