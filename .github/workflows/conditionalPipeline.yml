name: Experiment

on:
  push:
    branches:
      - 'master'
    paths:
      - 'GithubExperiment/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name
      run: |
        echo "Vasu"
