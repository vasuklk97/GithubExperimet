name: Experiment

on:
  push:
    branches:
      - 'master'
    paths:
      - '/GithubExperimet/.github/workflows/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name
      run: |
        echo "Vasu"
