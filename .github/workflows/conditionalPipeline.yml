name: My Reusable Workflow

on:
  workflow_call:
    secrets:
      NAME:
        required: true

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:

      - name: Run My Action
        env:
          Name: ${{ secrets.NAME }}
        run: |
          # Use the environment variable in your action
          echo "My secret API key is: ${{ Name }}"
