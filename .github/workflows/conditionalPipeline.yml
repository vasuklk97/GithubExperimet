name: Experiment

on:
  push:
    branches: [ 'master', 'FirstBranch', 'conditionalOutput']
    paths:
      - '.github/workflows/**'
  
    
      
  workflow_dispatch:
    inputs:
      actions:
        type: choice
        required: true
        default: Vasu
        description: Name
        options:
          - Vasu
          - Vishnu


jobs:
  build:
    outputs: 
      nameTag: |
        ${{ steps.setVasu.outputs.first || (steps.setVishnu.outputs.first)}}
    runs-on: ubuntu-latest
    steps:
    - name: Echo Name Vasu
      id: setVasu
      if: ${{ github.event.inputs.actions == 'Vasu' }}
      run: |
        echo "::set-output name=first::${{ github.event.inputs.actions }}
        
    - name: Echo Name Vishnu
      id: setVishnu
      if: ${{ github.event.inputs.actions == 'Vishnu' }}
      run: |
        echo "::set-output name=first::${{ github.event.inputs.actions }}
        
  job2:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
    - name: Print name
      run: |
        echo ${{ needs.build.outputs.nameTag }}
      
